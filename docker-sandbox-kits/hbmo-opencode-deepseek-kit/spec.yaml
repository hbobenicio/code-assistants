schemaVersion: "1"
kind: sandbox
name: hbmo-opencode
sandbox:
  image: "docker/sandbox-templates:opencode"
  entrypoint:
    run: [opencode, "--print-logs"]

network:
  allowedDomains:
    - "opencode.ai:443"
    - "api.deepseek.com:443"

